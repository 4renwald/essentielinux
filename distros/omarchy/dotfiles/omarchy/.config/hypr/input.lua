-- Keep only your personal input overrides here. Uncommented settings below
-- replace Omarchy's defaults.

-- Caps Lock is a real Caps Lock here: Omarchy's default makes it the compose
-- key, so drop that remap and keep the both-Shifts Caps Lock companion.
hl.config({
  input = {
    kb_options = "shift:both_capslock_cancel",
  },
})
